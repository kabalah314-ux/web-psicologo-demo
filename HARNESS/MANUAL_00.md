# Comprobaciones manuales — Fase 0

- [ ] `cd backend && uvicorn app.main:app --reload` arranca sin errores y http://localhost:8000/docs abre.
- [ ] `cd frontend && npm run dev` abre http://localhost:5173 sin errores en consola.
- [ ] `backend/.env` NO aparece en `git status`.